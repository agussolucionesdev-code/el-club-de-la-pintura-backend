# Install all dependencies (including devDependencies) in all environments.
# Required for Render deployments where NODE_ENV=production is set by default,
# which would otherwise cause npm to skip devDependencies like TypeScript.
production=false
